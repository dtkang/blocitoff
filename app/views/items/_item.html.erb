<%= content_tag :tr, id: "item-#{item.id}" do %>
  <td><%= item.name %></td>
  <td><%= distance_of_time_in_words(Time.now, item.created_at) %></td>
  <td><%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote:true %></td>
<% end %>